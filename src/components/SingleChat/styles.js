import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    mainView: {
        padding: 10,
    },
    mainText: {
        color: '#000',
        paddingBottom: 20,
    },
    boldWords: {
        fontWeight: '600',
        color: '#000',
    }
});

export default styles;
